import React from "react";

const displaydProduct = (props) => {
//   // console.log(props);
//   const display = props.propData.map((list) => {
//     return <option key={list.id}>{list.title}</option>;
//   });

// //   props.trickynews = {list.title};
//   return (
//     <>
//       <select>{display}</select>
//     </>
//   );
};

export default displaydProduct;
